export type TUser = {
  username?: string;
  email: string;
  password: string;
};

export type TProduct = {
  id: number;
  title: string;
  description: string;
  price: number;
  thumbnail: string;
};
